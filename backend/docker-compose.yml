version: '3.8'

services:
  # FastAPI 애플리케이션 서비스 (호스트 DB에 연결)
  web:
    build: .
    container_name: verde-fastapi
    restart: always
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    # 🚨 호스트 머신 접근을 위한 extra_hosts 설정
    extra_hosts:
      - "host.docker.internal:host-gateway"
  # 2. PostgreSQL 데이터베이스 서비스
  db:
    image: postgres:16-alpine # 가볍고 안정적인 최신 PostgreSQL 이미지 사용
    container_name: verde-postgres-db
    restart: always
    # .env 파일의 환경 변수를 DB 설정에 사용
    env_file:
      - .env
    # 데이터를 영구 저장할 볼륨 설정 (컨테이너가 삭제되어도 데이터는 유지)
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    # 5432 포트를 호스트에 노출 (선택 사항: 호스트에서 DB 툴로 접근할 때 필요)
    ports:
      - "5432:5432"

# 볼륨 정의
volumes:
  postgres_data: